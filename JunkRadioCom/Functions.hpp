#ifndef ROCKETPI_FUNCTIONS
#define ROCKETPI_FUNCTIONS

double mean(double arr[], int N);
int meanMulti(double* avgs, double arr[], int samples_read_per_channel, int num_channels);

#endif // ROCKETPI_FUNCTIONS